doctype html
html
    head
        meta(charset="UTF-8")
        meta(content="width=device-width, minimum-scale=1, maximum-scale=1", name="viewport")
        title
            block title
                title #{site.title}
                //- if file.title
                //-     | #{file.title} – #{site.title}
                //- else
                //-     | #{site.title}
    
        block head
    body
        block container
        +b.menu: +i